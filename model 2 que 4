#include<stdio.h>
int main()
{
	int i, p,q,r;
	printf("enter the values of p,q and r");
	scanf("%d %d %d",&p,&q,&r);
	printf("number from %d to %d except %d",p,q,r);
	for(i=p;i<=q;i++)
	{
		if(i%10==r|| i/10==r)
		{
			continue;
		}
		printf("%d\t",i);
	}
	return 0;
}
